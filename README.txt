# Payslip Validation Test Suite

This project contains a set of automated tests to validate the consistency, structure, and correctness of payroll data across multiple Excel files (`GTN.xlsx`, `Payrun.xlsx`) and the `mapping.json` file.

### Technologies Used
- Python 3.10+
- `pandas` for data handling
- `unittest` for testing
- Excel files (`.xlsx`)
- JSON configuration (`mapping.json`)

---

## Folder Structure

Each folder contains:
- `GTN.xlsx`
- `Payrun.xlsx`
- `mapping.json` 
- `testCaseX.py` — the Python test file (x is for the number of the test)

---

##  How to Run

1. Make sure you have Python 3.10+ installed
2. Install required packages:

```bash
pip install -r requirements.txt


